main(integer x) (
  var []integer tab;
  var integer i;
  i := 0;
  tab := [x]integer;
  while (i < x + 1) (
    tab[i] := i + 49;
    i++;
  );

  i := 0;
  while (i < x) (
    print(tab[i]);
    i++;
  );
)
