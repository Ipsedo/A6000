main(integer x) (
  var []integer tab;
  var integer i;

  tab := [x]integer;

  for (i := 0; i < arr_length(tab); i++ ) (
    tab[i] := i + 49;
  );

  for (i := 0; i < arr_length(tab); i++ ) (
    print(tab[i]);
  );
)
