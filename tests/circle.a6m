main(integer x) (
  var integer i;
  var integer j;
  var integer k;
  var boolean continue;

  continue := true;
  i := 0;
  k := 50;

  while continue (
    continue := false;
    j := 0;
    while j < k+1 (
      if i*i + j*j < k*k then (
        print(46);
  continue := true;
      ) else (
        print(35);
      );
      print(32);
      j := j+1;
    );
    print(10);
    i := i+1;
  );
)