main(integer x) (
  print(plus_vingt(x));
)

integer plus_deux(integer i) (
  result := i + 2;
)

integer plus_vingt(integer i) (
  var integer cpt;
  cpt := 0;
  result := i;
  while (cpt < 10) (
    result := plus_deux(result);
    cpt++;
  );
)
