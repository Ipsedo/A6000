main(integer x) (
  var integer i;
  var integer j;
  j := 0;
  if 1 <= 2 then (
  while j < 10 (
  	i := 0;
  	while i < 10 (
  		print(i + 48);
  		i := i + 1;
  	);
  	if 50 < i * i then (
  		print(58);
  	) else (

  	);
  	print(10);
  	j := j + 1;
  );
  ) else (

  );
)
