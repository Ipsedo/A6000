main(integer x) (
  var [][]integer tab;
  var integer j;
  var integer i;

  tab := [5 + 1 + 1 + 1 + 1 + 1][]integer;

  for (j := 0; j < arr_length(tab); j++) (
    tab[j] := [x]integer;
    for (i := 0; i < arr_length(tab[j]); i++ ) (
      tab[j][i] := j + i + 49;
    );
  );

  for(j:=0;j<arr_length(tab);j++) (
    for (i := 0; i < arr_length(tab[j]); i++ ) (
      print(tab[j][i]);
    );
    print(10);
  );
)
