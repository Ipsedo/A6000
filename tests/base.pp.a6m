
main(integer x) (
  var integer i;
  var integer j;
  var integer k;

  if 3 != 2 && 3*4 == 15 - 3 || 1 < 2 then (
  for j := 512; j > 0; j /= 2 (
    k := 0;
  	for i := 1; i < 1024; i *= 2 (
  		print(k + 48);
      k++;
  	);
  	if 50 < i * i then (
  		print(95);
  	);
  	print(10);
  );
  );
  print(10);
  for i := 9; i > 0; i-- (
    print(i + 48);
  );
  print(10);

  for i := 9; i >= 0; i-- (
    print(i + 48);
  );
  print(10);

  for i := 9; i >= 0; i-=3 (
    print(i + 48);
  );
  print(10);

  j:= 0;
  for i := 0; j < 10; i+=2 (
    print(j + 48);
    j+=2;
  );
  print(10);
  i := 48; i := i + 2; print(i);
)

